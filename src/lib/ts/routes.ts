// ============================================
const ROUTES = {
	home: { path: "/", label: "HOME" },
	docker: { path: "/docker", label: "RUNNING WITH DOCKER" },
	config: { path: "/config", label: "CONFIG FILE" }
} as const;

export default ROUTES;
// ============================================
